'use strict';

angular.module('steps2makeApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomePeople = [
      'Makers',
      'Builders',
      'Cooks',
      'and DYI addicts'
    ];
  });
